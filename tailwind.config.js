/** @type {import('tailwindcss').Config} */
export default {
	content: [],
	theme: {
		extend: {
			colors: {
				// colors from the project ----------
				main: 'var(--nain)',
				mainHover: 'var(--hover)',
				dark: 'var(--dark)',
				darkGrey: 'var(--dark-grey)',
				grey: 'var(--grey)',
				// ------------------------
			},
		},
	},
	plugins: [],
};
